# Dependency changes are reviewed code changes. See README "Supply chain".
save-exact=true
# Do not resolve to a dependency release younger than this many days, so a
# compromised publish has time to be caught before it can land here.
min-release-age=2
